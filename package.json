{
  "name": "zeph-plugin",
  "version": "0.12.1",
  "private": true,
  "description": "Zeph Claude Code plugin — hooks, skills, and behavioral rules",
  "scripts": {
    "lint:rules-sync": "node scripts/lint-rules-sync.js"
  }
}
